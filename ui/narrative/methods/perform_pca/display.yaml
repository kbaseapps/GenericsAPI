name     : Perform PCA Analysis
subtitle : Perform PCA analysis on Matrix object
tooltip  : Perform PCA analysis on Matrix object

icon: analysis.png

screenshots :
    []

suggestions :
    apps:
        related :
            []
        next :
            []
    methods:
        related :
            []
        next :
            []

parameters :
    input_obj_ref :
        ui-name: |
            Matrix Object
        short-hint : |
            KBaseMatrices object
        long-hint  : |
            KBaseMatrices object

    n_components :
        ui-name : |
            Number of Components
        short-hint : |
            Number of Components

    dimension:
        ui-name: |
            Perform PCA Analysis On
        short-hint : |
            Perform PCA on column or row
        long-hint  : |
            Perform PCA on column or row

    attribute_mapping_obj_ref:
        ui-name: |
            AttributeMapping Object
        short-hint : |
            Select associated KBaseExperiments.AttributeMapping object
        long-hint  : |
            Select associated KBaseExperiments.AttributeMapping object

    associated_matrix_obj_ref:
        ui-name: |
            Associated Matrix Object
        short-hint: |
            Associated Matrix object used to group and size result marker
        long-hint: |
            Associated Matrix object used to group and size result marker

    attribute_color:
        ui-name: |
            Attribute
        short-hint : |
            Select an attribute for plot coloring
        long-hint  : |
            Select an attribute for plot coloring

    attribute_size:
        ui-name: |
            Attribute
        short-hint : |
            Select an attribute for plot sizing
        long-hint  : |
            Select an attribute for plot sizing

    row_size:
        ui-name: |
            Associated Matrix Row
        short-hint: |
            Select a row from associated matrix for plot sizing (valid when perform MDS on Column)
        long-hint: |
            Select a row from associated matrix for plot sizing (valid when perform MDS on Column)

    col_size:
        ui-name: |
            Associated Matrix Column
        short-hint: |
            Select a column from associated matrix for plot sizing (valid when perform MDS on Row)
        long-hint: |
            Select a column from associated matrix for plot sizing (valid when perform MDS on Row)


    pca_matrix_name:
        ui-name: |
            PCA Matrix Object Name
        short-hint : |
            Output KBaseExperiments.PCAMatrix object name
        long-hint  : |
            Output KBaseExperiments.PCAMatrix object name

parameter-groups :
    color_marker_by:
        ui-name : Group Marker By
        short-hint :  Select an attribute for plot coloring
    scale_size_by:
        ui-name : Scale Marker Size By
        short-hint :  Select an attribute for plot sizing



description : |
    <p> Perform PCA analysis on Matrix object </p>
